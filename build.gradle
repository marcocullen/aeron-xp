plugins {
    id 'java'
    id 'com.github.johnrengelman.shadow' version '8.1.1'
}

group = 'org.starquake'
version = '0.0.1'

repositories {
    mavenLocal()
    mavenCentral()
}

dependencies {
    implementation 'io.aeron:aeron-client:1.47.2-EXPERIMENTAL'
    implementation 'io.aeron:aeron-archive:1.47.2-EXPERIMENTAL'
    implementation 'io.aeron:aeron-driver:1.47.2-EXPERIMENTAL'
    implementation 'org.codejargon.feather:feather:1.0'
    implementation 'org.yaml:snakeyaml:2.2'  // Just the YAML parser
    implementation 'io.micrometer:micrometer-core:1.11.5'
    testImplementation platform('org.junit:junit-bom:5.10.0')
    testImplementation 'org.junit.jupiter:junit-jupiter'

}

shadowJar {
    // Optional: customize the base name of the jar
    archiveBaseName.set('aeron-app')
    manifest {
        attributes(
            'Main-Class': 'org.starquake.HorizonArchive'
        )
    }

    // Optional: if you need to relocate any dependencies to avoid conflicts
    // relocate 'com.google.common', 'shadow.com.google.common'

    // Ensure the manifest has the correct attributes
    manifest {
        attributes(
                'Main-Class': 'org.starquake.driver.AeronDriver'  // This can be any of your main classes since we'll override in Docker
        )
    }
}

test {
    useJUnitPlatform()
}
